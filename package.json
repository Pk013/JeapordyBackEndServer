{
  "name": "jeopardybackend",
  "version": "1.0.0",
  "description": "Changing the method to pull from backend server",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Pk Shannon",
  "license": "ISC",
  "dependencies": {
    "express": "^4.16.3"
  }
}
